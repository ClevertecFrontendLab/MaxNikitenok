import { createSlice } from '@reduxjs/toolkit';

export const authSlice = createSlice({
    name: 'auth',
    initialState: {
        name: 'ttt',
    },
    reducers: {},
    // extraReducers: (builder) => {},
});
